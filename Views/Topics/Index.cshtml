@model IEnumerable<CodeninModel.Quiz.Topic>

@{
    ViewBag.Title = "Topics";
    ViewBag.Topics = "class = active";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<div class="container-fluid" style="margin-left: 13%">
    <h2>Topics</h2>
    <br/>
    
    @*@using (Html.BeginForm("ExcelUpload", "Modules", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()
        <input class="form-control" type="file" id="File" name="excelfile" class="btn btn-default btn-md" />

        <input type="submit" value="submit">

    }*@   
    

    <p>
            @Html.ActionLink("CREATE TOPICS", "Create", null, new{@class = "my-class; btn"})
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Module.ModuleName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TopicName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ExpectedTime)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Module.ModuleName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TopicName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ExpectedTime)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.TopicId }, new { @class = "btn" })
                        @Html.ActionLink("Details", "Details", new { id = item.TopicId }, new { @class = "btn" })
                        @Html.ActionLink("Delete", "Delete", new { id = item.TopicId }, new { @class = "btn" })
                    </td>
                </tr>
            }

        </table>
        
    @*<div class="table container" style="width: 99%; margin: 0 auto">
            <a onclick="myFunction()" class="btn btn-primary btn-lg"> <i class="fa fa-plus left"></i> Add Topic</a>

            <table id="Jdatatable" class="table table-hover">
                <thead>
                <tr>
                    <th>Module Name</th>
                    <th>Topic Name</th>
                    <th>Expected Time</th>                 
                    <th>Actions</th>                  
                </tr>
                </thead>
            </table>
        </div>
        <!-- Modal -->
        <div class="modal fade modal-bf-fh" id="actionsList" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <!--Content-->
                <div class="modal-content">
                    <!--Header-->
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                        <div class="flex-center">
                            <h4 class="modal-title">Topic</h4>
                        </div>
                    </div>
                    <!--Body-->
                    <div class="modal-body" id="myModalDivBody"></div>
                </div>
                <!--/.Content-->
            </div>
        </div>
        
        <script src="~/Scripts/jquery-3.1.1.min.js"></script>

        <script type="text/javascript">
            $(document).ready(function() {              
                window.oTable = $("#Jdatatable").DataTable({
                    "processing": true, // for show progress bar while loading
                    "serverSide": true, // if it is true the preocess is done on server side
                    "filter": true, // disables search box
                    "info": true,
                    "stateSave": true,
                    "orderMulti": false, // disable multiple column loading at once
                    "ajax": {
                        "url": "/Topics/GetIndex",
                        "type": "POST",
                        "datatype": "json"
                    },
                    "columns": [
                        { "data":"TopicId", "autoWidth": true},
                        { "data": "ModuleName", "autoWidth": true },
                        { "data": "TopicName", "autoWidth": true },
                        { "data": "ExpectedTime", "autoWidth": true },
                        

                        {
                            "data": "TopicId",
                            "autoWidth": true,
                            "render": function(data) {
                                return '<a href="#" onclick="Edit('+data+')" ><i class="fa fa-pencil" aria-hidden="true" ><i/></a>' +
                                    '<a class="text-danger" href="#" onclick="Delete('+data+')" ><i class="fa fa-remove" aria-hidden="true" ><i/></a>';
                            }
                        }
                        
                    ]
                });

       
            });
            function Edit(subjecId) {
                var url = "/Topics/Save?id=" + subjecId;              
                $('#myModalDivBody').load(url,
                    function () {
                        $('#actionsList').modal("show");
                    });
            };
            function Delete(subjecId) {
                var url = "/Topics/Delete?id=" + subjecId;             
                $('#myModalDivBody').load(url,
                    function () {
                        $('#actionsList').modal("show");
                    });
            };
            function myFunction() {
                var url = "/Topics/Save?id=0";
                $('#myModalDivBody').load(url,
                    function () {
                        $('#actionsList').modal("show");
                    });
            };
        </script>
    </div>*@

</div>
